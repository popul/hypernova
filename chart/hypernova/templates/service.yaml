apiVersion: v1
kind: Service
metadata:
  name: {{ include "hypernova.fullname" . }}
  labels:
    {{- include "hypernova.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "hypernova.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
